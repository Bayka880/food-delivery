import React from "react";

export default function CardModal() {
  /*********************   Card Modal    *******************************
   *
   *   * Picture
   *   * Title and price
   *   * Орц followed with text area that has the ingredients
   *   * Тоо followed with - + radio buttons and the current selected number
   *   * Сагслах is button which suppose to send currently selected food
   *     info to add-item-to-basket in back-end
   *   * if successfully added to the basket close the modal and open cardSideBar
   *   * backdrop or x buttons will close the modal!
   *   * every card suppose to listen click and open up modal with the targetted
   *     food info
   **********************************************************************/
  return <div>CardModxal</div>;
}
