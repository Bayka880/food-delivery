import React from "react";

export default function OrderHistory() {
  /*********************   Order History   *************************************
   *   * Get all the data from back-end (Do not forget to show loading screen while waiting back-end response)
   *   * in the left-side list all the order history data
   *   * in the right-side show the information about order which has selected from left-side
   **********************************************************************/
  return <div>OrderHistory</div>;
}
